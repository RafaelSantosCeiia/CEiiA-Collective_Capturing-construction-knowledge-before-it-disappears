# --- núcleo (sempre necessário) ---
typer>=0.12
rich>=13.7
pandas>=2.2
numpy>=1.26
pyarrow>=15
pydantic>=2.6
python-dotenv>=1.0
pymupdf>=1.24          # split dos PDFs de processo (fitz)

# --- modelação / treino ---
scikit-learn>=1.4
joblib>=1.4
optuna>=3.6
lightgbm>=4.3
xgboost>=2.0
catboost>=1.2

# --- extração de geometria (live) ---
google-genai>=0.3      # provider default (Gemini 3.5 Flash); export GEMINI_API_KEY
# anthropic>=0.40       # opcional: provider claude
# autogluon.tabular>=1.0  # opcional: entra no benchmark se instalado

# --- API HTTP (frontend) ---
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
